use anyhow::*;
use itertools::Itertools;

pub static DAY: u32 = <DAY>;
pub static EXAMPLE_INPUT: &str = "<EXAMPLE INPUT>";

pub fn main(input: &str) -> Result<(String, String)> {
    println!("{}", input);
    Ok(("Not implemented".into(), "Not implemented".into()))
}
